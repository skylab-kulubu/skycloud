services:
  jenkins:
    image: jenkins/jenkins:lts
    ports:
      - 8080:8080
    container_name: jenkins
    volumes:
      - jenkins_home:/var/jenkins_home
    networks:
      skycloud-network:
networks:
  skycloud-network:
    external: true
volumes:
  jenkins_home:
